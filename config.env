# =============================================================================
# GPT Researcher 环境配置文件
# =============================================================================

# DeepSeek API 配置 (替换 OpenAI)
DEEPSEEK_API_KEY=
# LLM 配置 - 使用 DeepSeek 模型
FAST_LLM=deepseek:deepseek-chat
SMART_LLM=deepseek:deepseek-chat
STRATEGIC_LLM=deepseek:deepseek-chat

# 搜索引擎配置 (推荐使用 Tavily，需要申请免费 API)
TAVILY_API_KEY=
RETRIEVER=tavily

# 可选：如果不使用 Tavily，可以使用免费的 DuckDuckGo
# RETRIEVER=duckduckgo

# 嵌入模型配置 (保持使用 OpenAI 的嵌入模型，性能较好)
# 如果您有 OpenAI API Key，建议保留用于嵌入
OPENAI_API_KEY=your_openai_api_key_for_embeddings_only
EMBEDDING=openai:text-embedding-3-small

# 如果不想使用 OpenAI 嵌入，可以使用其他选项
# EMBEDDING=huggingface:sentence-transformers/all-MiniLM-L6-v2

# 报告配置
TOTAL_WORDS=1500
REPORT_FORMAT=APA
LANGUAGE=chinese

# 其他配置
TEMPERATURE=0.7
MAX_SEARCH_RESULTS_PER_QUERY=5
# VERBOSE=true  # 注释掉 VERBOSE，使用默认值

# 本地文档路径 (可选)
DOC_PATH=./my-docs

# 代理配置 (如果需要)
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
